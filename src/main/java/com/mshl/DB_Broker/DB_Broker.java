package com.mshl.DB_Broker;

public class DB_Broker
{

    /**
     * Создает диалог пользователей.
     * @return Возвращается id нового диалога. Если возникнет ошибка в БД, то
     * возвращается -1.
     */
    public int CreateDialog(int user1_id, int user2_id)
    {
        return 0;
    }
}
